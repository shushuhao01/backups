# 财务管理批量搜索缺失关键词提示功能同步

## 任务日期
2026-03-25

## 任务概述
将增值管理（ValueAddedManage）的批量搜索缺失关键词提示功能同步到绩效管理（PerformanceManage）、绩效数据（PerformanceData）和代收管理（CodCollection）三个模块。

## 功能说明

### 增值管理已有功能（参考源）
增值管理的批量搜索框在搜索后，会自动计算哪些搜索关键词未在返回结果中匹配到，并在搜索框右侧显示一个"缺X"的标签。鼠标悬浮该标签后，弹出浮层展示：
1. 缺失条数汇总
2. 缺失关键词列表
3. "一键复制"按钮，可将所有缺失关键词复制到剪贴板

### 同步到的模块
- **绩效管理** (`PerformanceManage.vue`)
- **绩效数据** (`PerformanceData.vue`)
- **代收管理** (`CodCollection.vue`)

## 修改文件清单

### 1. `src/views/Finance/PerformanceManage.vue`

#### 模板修改
- 在搜索框 `<template #suffix>` 中添加缺失关键词的 `<el-popover>` 弹出层
- 包含"缺X"标签、缺失列表、一键复制按钮

#### 脚本修改
- 导入 `DocumentCopy` 图标组件
- 新增 `missingKeywords` ref 变量
- 新增 `computeMissingKeywords()` 函数 - 在 `loadData` 完成后比对搜索关键词与返回数据
- 新增 `copyMissingKeywords()` 函数 - 支持 `navigator.clipboard` API，带 `document.execCommand` 降级方案
- 在 `loadData()` 中数据加载完成后调用 `computeMissingKeywords()`
- 在 `clearBatchSearch()` 中清除 `missingKeywords`
- 匹配字段：`orderNumber`、`customerName`、`trackingNumber`

#### 样式修改
- 新增 `.missing-count-tag` 样式（灰色圆角标签，hover 变为橙色）
- 新增 `.missing-popover` 及其子元素样式
- 新增 `.missing-popover-header` 样式（flex 布局，标题加一键复制按钮）
- 新增 `.missing-popover-list` 和 `.missing-item` 样式

### 2. `src/views/Finance/PerformanceData.vue`

#### 修改内容
- 与 PerformanceManage.vue 完全一致的功能同步
- 导入 `DocumentCopy` 图标组件
- 新增模板、脚本逻辑、样式（同上）
- 匹配字段：`orderNumber`、`customerName`、`trackingNumber`

### 3. `src/views/Finance/CodCollection.vue`

#### 修改内容
- 导入 `DocumentCopy` 图标组件
- 搜索框添加 `batch-badge` 和缺失关键词弹出层
- 搜索框 placeholder 改为 `'批量搜索（点击展开）'`（与其他模块统一）
- 新增 `missingKeywords` ref 变量
- 新增 `computeMissingKeywords()` 和 `copyMissingKeywords()` 函数
- 在 `loadData()` 中调用 `computeMissingKeywords()`
- 在 `clearBatchSearch()` 中清除 `missingKeywords`
- 匹配字段：`orderNumber`、`customerName`、`customerPhone`、`trackingNumber`（代收管理额外支持手机号匹配）

## 技术实现细节

### 核心逻辑
```typescript
// 搜索缺失关键词 ref
const missingKeywords = ref<string[]>([])

// 计算缺失关键词（在 loadData 后调用）
const computeMissingKeywords = () => {
  if (!batchSearchKeywords.value || batchSearchCount.value === 0) {
    missingKeywords.value = []
    return
  }
  const keywords = batchSearchKeywords.value
    .split(/[\n,;，；]+/)
    .map(k => k.trim())
    .filter(k => k.length > 0)
  const missing: string[] = []
  for (const kw of keywords) {
    const kwLower = kw.toLowerCase()
    const found = tableData.value.some(row =>
      (row.orderNumber && row.orderNumber.toLowerCase().includes(kwLower)) ||
      (row.customerName && row.customerName.toLowerCase().includes(kwLower)) ||
      (row.trackingNumber && row.trackingNumber.toLowerCase().includes(kwLower))
    )
    if (!found) missing.push(kw)
  }
  missingKeywords.value = missing
}

// 一键复制缺失关键词
const copyMissingKeywords = async () => {
  if (missingKeywords.value.length === 0) return
  try {
    await navigator.clipboard.writeText(missingKeywords.value.join('\n'))
    ElMessage.success(`已复制 ${missingKeywords.value.length} 条缺失内容`)
  } catch {
    // fallback 方案
    const textarea = document.createElement('textarea')
    textarea.value = missingKeywords.value.join('\n')
    document.body.appendChild(textarea)
    textarea.select()
    document.execCommand('copy')
    document.body.removeChild(textarea)
    ElMessage.success(`已复制 ${missingKeywords.value.length} 条缺失内容`)
  }
}
```

### 模板结构
```html
<template #suffix>
  <el-badge v-if="batchSearchCount > 0" :value="batchSearchCount" :max="999" class="batch-badge" />
  <el-popover v-if="missingKeywords.length > 0" placement="bottom" :width="360" trigger="hover">
    <template #reference>
      <span class="missing-count-tag">缺{{ missingKeywords.length }}</span>
    </template>
    <div class="missing-popover">
      <div class="missing-popover-header">
        <span>以下 <b>{{ missingKeywords.length }}</b> 条未匹配到结果</span>
        <el-button type="primary" link size="small" @click="copyMissingKeywords">
          <el-icon><DocumentCopy /></el-icon> 一键复制
        </el-button>
      </div>
      <div class="missing-popover-list">
        <div v-for="(kw, idx) in missingKeywords" :key="idx" class="missing-item">
          <span class="missing-item-text">{{ kw }}</span>
        </div>
      </div>
    </div>
  </el-popover>
</template>
```

## 注意事项
1. 缺失关键词的匹配是基于当前页面返回的数据（`tableData`），而非全量数据。如果后端分页返回的数据中未包含某条记录，该关键词也会被标记为"缺失"。这与增值管理的行为一致。
2. 复制功能优先使用现代的 `navigator.clipboard` API，如果不可用（如 HTTP 环境）则降级使用 `document.execCommand('copy')`。
3. 各模块匹配的字段根据其业务数据结构有所不同：
   - 绩效管理/绩效数据：`orderNumber`、`customerName`、`trackingNumber`
   - 代收管理：`orderNumber`、`customerName`、`customerPhone`、`trackingNumber`

## 测试步骤
1. 进入绩效管理/绩效数据/代收管理页面
2. 点击批量搜索输入框，输入多行搜索内容（包含一些不存在的关键词）
3. 点击"搜索"按钮
4. 验证搜索框右侧是否出现"缺X"标签
5. 鼠标悬浮"缺X"标签，验证弹出层显示缺失关键词列表
6. 点击"一键复制"按钮，验证剪贴板内容
7. 点击"清空"按钮，验证"缺X"标签消失

