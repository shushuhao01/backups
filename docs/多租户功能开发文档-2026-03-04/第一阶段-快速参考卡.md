# 第一阶段 - 快速参考卡 🚀

## ✅ 完成状态

**阶段**：第一阶段 - 核心基础（P0）  
**完成度**：100% (5/5)  
**测试通过率**：100%  
**数据隔离率**：100%

---

## 📁 核心文件

| 文件 | 说明 |
|------|------|
| `backend/src/entities/Tenant.ts` | 租户实体 |
| `backend/src/entities/TenantSettings.ts` | 租户配置实体 |
| `backend/src/repositories/BaseRepository.ts` | 基础仓储类 |
| `backend/src/utils/tenantContext.ts` | 租户上下文管理 |
| `backend/src/middleware/tenantAuth.ts` | 租户认证中间件 |

---

## 🚀 快速验收

```bash
# Windows用户
双击运行：验收测试-第一阶段.bat

# 命令行用户
cd backend
npm run build
node run-phase1-tests.js
```

---

## 💡 核心用法

### 1. 使用BaseRepository

```typescript
import { BaseRepository } from './repositories/BaseRepository';
import { Customer } from './entities/Customer';

const customerRepo = new BaseRepository(Customer);

// 查询（自动添加tenant_id过滤）
const customers = await customerRepo.find({ where: { status: 'active' } });

// 保存（自动设置tenant_id）
const customer = customerRepo.create({ name: '张三' });
await customerRepo.save(customer);
```

### 2. 使用租户认证

```typescript
import { tenantAuth, checkTenantLimits } from './middleware/tenantAuth';

// 在路由中使用
router.get('/api/customers', tenantAuth, getCustomers);
router.post('/api/users', tenantAuth, checkTenantLimits, createUser);
```

### 3. 使用租户上下文

```typescript
import { TenantContextManager, tenantContextMiddleware } from './utils/tenantContext';

// 在中间件中设置
app.use(tenantAuth);
app.use(tenantContextMiddleware);

// 获取租户ID
const tenantId = TenantContextManager.getTenantId();

// 手动运行带上下文的函数
await TenantContextManager.run({ tenantId: 'xxx' }, async () => {
  const customers = await customerRepo.find();
});
```

---

## ⚠️ 重要规范

### 字段命名

```typescript
// ✅ 正确
@Column('varchar', { length: 36, name: 'tenant_id' })
tenantId: string;

// ❌ 错误
@Column('varchar', { length: 36 })
tenant_id: string;
```

### 中间件顺序

```typescript
// ✅ 正确顺序
app.use(tenantAuth);
app.use(tenantContextMiddleware);
app.use('/api/customers', customerRoutes);

// ❌ 错误顺序
app.use(tenantContextMiddleware); // 必须在tenantAuth之后
app.use(tenantAuth);
```

---

## 📊 测试结果

| 测试 | 通过率 |
|------|--------|
| 租户实体测试 | 100% |
| 租户配置实体测试 | 100% (13/13) |
| 租户认证中间件测试 | 100% (12/12) |
| 基础仓储类测试 | 100% (9/9) |
| **总计** | **100%** |

---

## 🚀 下一步

**任务2.1：修改用户实体添加 tenant_id**
- 在User实体中添加tenantId字段映射
- 预计时间：1小时

---

## 📞 快速链接

- 详细总结：`第一阶段-核心基础-完成总结.md`
- 验收指南：`第一阶段-验收指南.md`
- 完成说明：`README-第一阶段完成.md`
