# 多租户系统完整实施方案 - 第3部分：实施步骤和时间规划

## 🚀 完整实施步骤

基于对CRM项目的深入分析，制定以下详细的实施计划。

---

## 阶段1: 数据库改造（Week 1）⭐ 最优先

### Day 1-2: 设计和规划

#### 任务清单
- [ ] 确认需要添加tenant_id的32个业务表
- [ ] 确认不需要添加tenant_id的7个系统表
- [ ] 设计租户相关表结构（tenants, tenant_settings）
- [ ] 编写数据库迁移脚本
- [ ] 准备回滚方案

#### 交付物
```sql
-- backend/database-migrations/add-tenant-support.sql
-- 已创建，包含：
-- 1. 创建租户表（tenants, tenant_settings）
-- 2. 所有业务表添加tenant_id字段
-- 3. 修改唯一索引支持租户隔离
-- 4. 创建复合索引优化查询性能
```

### Day 3-4: 执行迁移

#### 开发环境测试
```bash
# 1. 备份数据库
mysqldump -u root -p crm_dev > backup_before_migration.sql

# 2. 执行迁移脚本
mysql -u root -p crm_dev < backend/database-migrations/add-tenant-support.sql

# 3. 验证迁移结果
mysql -u root -p crm_dev < backend/database-migrations/verify-migration.sql
```

#### 验证清单
- [ ] 所有业务表已添加tenant_id字段
- [ ] 所有tenant_id字段已添加索引
- [ ] 唯一索引已修改为复合索引
- [ ] 租户表已创建成功
- [ ] 现有数据的tenant_id为NULL（私有部署模式）

### Day 5: 测试和文档

#### 测试项目
- [ ] 测试私有部署模式（tenant_id=NULL）
- [ ] 测试唯一索引（允许多个NULL值）
- [ ] 测试查询性能（添加索引后）
- [ ] 测试数据完整性

#### 文档更新
- [ ] 更新数据库Schema文档
- [ ] 编写迁移说明文档
- [ ] 编写回滚指南

---

## 阶段2: 后端租户隔离（Week 2）⭐ 核心功能

### Day 1-2: 租户认证

#### 任务清单
- [ ] 实现租户认证中间件（tenantAuth）
- [ ] 实现Token包含tenantId
- [ ] 实现租户状态验证
- [ ] 实现租户过期检查

#### 代码实现
```typescript
// backend/src/middleware/tenantAuth.ts
// 已创建，包含：
// 1. 租户认证中间件
// 2. 可选租户认证
// 3. 租户资源限制检查
// 4. 租户ID提取工具函数
```

#### 测试清单
- [ ] 测试Token包含tenantId
- [ ] 测试租户状态验证
- [ ] 测试租户过期检查
- [ ] 测试401/403错误处理

### Day 3-4: 数据隔离

#### 任务清单
- [ ] 创建BaseRepository（自动添加tenant_id）
- [ ] 修改所有Repository继承BaseRepository
- [ ] 实现智能查询（自动过滤tenant_id）
- [ ] 实现智能插入（自动添加tenant_id）

#### 代码实现
```typescript
// backend/src/repositories/BaseRepository.ts
export class BaseRepository<T> {
  constructor(
    private repository: Repository<T>,
    private req: TenantRequest
  ) {}

  // 智能查询 - 自动添加tenant_id过滤
  async find(options?: FindManyOptions<T>) {
    const where = options?.where || {}
    
    // SaaS模式: 自动添加tenant_id过滤
    if (deployConfig.isSaaS() && this.req.tenantId) {
      where['tenant_id'] = this.req.tenantId
    }
    
    return this.repository.find({
      ...options,
      where
    })
  }

  // 智能插入 - 自动添加tenant_id
  async create(data: any) {
    // SaaS模式: 自动添加tenant_id
    if (deployConfig.isSaaS() && this.req.tenantId) {
      data.tenant_id = this.req.tenantId
    }
    
    return this.repository.create(data).save()
  }
}
```

#### 迁移清单
- [ ] CustomerRepository → BaseRepository
- [ ] OrderRepository → BaseRepository
- [ ] ProductRepository → BaseRepository
- [ ] UserRepository → BaseRepository
- [ ] 其他所有Repository

### Day 5: 测试

#### 单元测试
```typescript
// backend/tests/tenant-isolation.test.ts
describe('租户数据隔离测试', () => {
  it('租户A不能查询到租户B的数据', async () => {
    // 测试代码
  })
  
  it('租户A不能修改租户B的数据', async () => {
    // 测试代码
  })
})
```

#### 测试清单
- [ ] 单元测试通过
- [ ] 数据隔离测试通过
- [ ] 性能测试通过
- [ ] 安全测试通过

---

## 阶段3: 租户管理功能（Week 3）

### Day 1-2: 租户创建和初始化

#### 任务清单
- [ ] 实现租户创建接口
- [ ] 实现租户初始化（默认数据）
- [ ] 实现授权码生成
- [ ] 实现租户登录接口

#### API接口
```typescript
// POST /api/v1/admin/tenants - 创建租户
// POST /api/v1/tenant-license/login - 租户登录
// POST /api/v1/tenant-license/verify - 验证授权码
```

#### 租户初始化数据
```typescript
async function initTenantData(tenantId: string) {
  // 1. 创建默认管理员账号
  await createDefaultAdmin(tenantId)
  
  // 2. 创建默认角色
  await createDefaultRoles(tenantId)
  
  // 3. 创建默认配置
  await createDefaultSettings(tenantId)
  
  // 4. 创建默认部门
  await createDefaultDepartment(tenantId)
}
```

### Day 3-4: 租户管理

#### 任务清单
- [ ] 实现租户列表查询
- [ ] 实现租户信息更新
- [ ] 实现租户状态管理（启用/禁用）
- [ ] 实现租户续期功能

#### API接口
```typescript
// GET /api/v1/admin/tenants - 租户列表
// GET /api/v1/admin/tenants/:id - 租户详情
// PUT /api/v1/admin/tenants/:id - 更新租户
// PUT /api/v1/admin/tenants/:id/status - 更新状态
// PUT /api/v1/admin/tenants/:id/renew - 续期
```

### Day 5: 租户配置

#### 任务清单
- [ ] 实现租户配置管理
- [ ] 实现租户资源限制
- [ ] 实现租户功能模块控制
- [ ] 测试租户管理功能

---

## 阶段4: 前端租户支持（Week 4）

### Day 1-2: 登录页面

#### 任务清单
- [ ] 修改登录页面（添加授权码输入）
- [ ] 实现租户登录逻辑
- [ ] 保存租户信息到localStorage
- [ ] 实现租户状态提示

#### 代码实现
```vue
<!-- src/views/Login.vue -->
<template>
  <el-form>
    <!-- SaaS模式: 显示授权码输入 -->
    <el-form-item v-if="isSaaS" label="授权码">
      <el-input v-model="form.licenseKey" placeholder="请输入租户授权码" />
    </el-form-item>
    
    <el-form-item label="用户名">
      <el-input v-model="form.username" />
    </el-form-item>
    
    <el-form-item label="密码">
      <el-input v-model="form.password" type="password" />
    </el-form-item>
  </el-form>
</template>
```

### Day 3-4: 请求处理

#### 任务清单
- [ ] 修改请求拦截器（Token包含tenantId）
- [ ] 处理租户过期提示
- [ ] 处理租户禁用提示
- [ ] 实现租户信息显示

#### 代码实现
```typescript
// src/api/request.ts
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  
  // Token中已包含tenantId,后端会自动提取
  return config
})

// 响应拦截器
request.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 403) {
      const message = error.response.data.message
      if (message.includes('租户已过期')) {
        showExpiredDialog()
      } else if (message.includes('租户已禁用')) {
        showDisabledDialog()
      }
    }
    return Promise.reject(error)
  }
)
```

### Day 5: 测试优化

#### 测试清单
- [ ] 功能测试
- [ ] 用户体验优化
- [ ] Bug修复
- [ ] 性能优化

---

## 阶段5: Admin后台核心功能（Week 5-6）

### Week 5: 基础功能

#### Day 1-2: 文件上传
- [ ] 实现版本文件上传接口
- [ ] 支持大文件上传
- [ ] 文件存储方案（本地/OSS）

#### Day 3-4: 权限控制
- [ ] 实现权限表设计
- [ ] 实现权限中间件
- [ ] 实现操作日志

#### Day 5: 数据导出
- [ ] 实现Excel导出
- [ ] 实现CSV导出
- [ ] 自定义字段选择

### Week 6: 完善功能

#### Day 1-2: 接口管理
- [ ] API列表展示
- [ ] API调用统计
- [ ] 性能监控

#### Day 3-4: 高级功能
- [ ] 高级搜索
- [ ] 批量操作
- [ ] 数据统计

#### Day 5: 测试上线
- [ ] 全面测试
- [ ] Bug修复
- [ ] 文档完善

---

## 阶段6: 私有部署适配（Week 7）

### Day 1-2: 配置和判断

#### 任务清单
- [ ] 添加DEPLOY_MODE环境变量
- [ ] 实现模式判断逻辑
- [ ] 修改中间件支持两种模式

#### 代码实现
```typescript
// backend/src/config/deploy.ts
// 已创建，包含：
// 1. 部署模式配置
// 2. 模式判断函数
// 3. 私有部署配置
// 4. SaaS配置
```

### Day 3-4: 授权验证

#### 任务清单
- [ ] 实现私有部署授权激活
- [ ] 实现授权验证接口
- [ ] 实现授权过期检查

#### API接口
```typescript
// POST /api/v1/license/activate - 激活授权
// GET /api/v1/license/verify - 验证授权
// GET /api/v1/license/info - 授权信息
```

### Day 5: 测试

#### 测试清单
- [ ] 测试私有部署模式
- [ ] 测试租户模式
- [ ] 测试模式切换
- [ ] 测试授权验证

---

## 时间规划总结

| 阶段 | 时间 | 优先级 | 状态 |
|------|------|--------|------|
| 数据库改造 | Week 1 | ⭐ 最高 | 待开始 |
| 后端租户隔离 | Week 2 | ⭐ 最高 | 待开始 |
| 租户管理功能 | Week 3 | ⭐ 最高 | 待开始 |
| 前端租户支持 | Week 4 | ⭐ 最高 | 待开始 |
| Admin后台核心 | Week 5-6 | 🟡 中等 | 待开始 |
| 私有部署适配 | Week 7 | ⭐ 最高 | 待开始 |
| **总计** | **7周** | - | - |

---

## 风险评估和应对

### 风险1: 数据库迁移失败
- **概率**: 低
- **影响**: 高
- **应对**: 
  - 充分测试迁移脚本
  - 准备回滚方案
  - 先在开发环境测试

### 风险2: 性能问题
- **概率**: 中
- **影响**: 中
- **应对**:
  - 添加合适的索引
  - 优化查询语句
  - 使用缓存

### 风险3: 数据泄露
- **概率**: 低
- **影响**: 高
- **应对**:
  - 严格的代码审查
  - 全面的测试
  - 安全审计

### 风险4: 时间延期
- **概率**: 中
- **影响**: 中
- **应对**:
  - 合理的时间缓冲
  - 优先完成核心功能
  - 灵活调整计划

---

**下一部分**: [第4部分 - 代码实现示例](./多租户系统完整实施方案-第4部分-代码示例.md)
