# 第四阶段：模块和系统设置 - 实施计划

**制定时间**: 2026-03-06  
**实施周期**: 5-7天  
**当前完成度**: 60%  
**目标完成度**: 100%

---

## 📊 当前状态

### 已完成功能 ✅
1. 系统配置基础框架（systemConfig.ts）
2. 短信配置API（100%）
3. 邮件配置API（100%）
4. 超时提醒配置API（100%）
5. 前端基础配置页面（Basic.vue）
6. 前端管理员用户页面（AdminUsers.vue）
7. 前端通知模板页面（NotificationTemplates.vue）
8. 前端模块列表页面（modules/List.vue）
9. 前端模块配置页面（modules/Config.vue）
10. 前端接口列表页面（api/List.vue）

### 待完成功能 ⏳
1. 模块管理后端API（0%）
2. 系统设置后端API（0%）
3. 管理员用户管理API（0%）
4. 接口管理后端API（0%）
5. 前后端对接和测试（0%）

---

## 🎯 实施目标

### 核心目标
1. 完成模块管理的完整后端API
2. 完成系统设置的完整后端API
3. 完成管理员用户管理API
4. 完成接口管理的完整后端API
5. 前后端完全对接并测试通过
6. 达到生产就绪状态

### 质量目标
- API响应时间 < 500ms
- 测试覆盖率 > 80%
- 代码审查通过率 100%
- 文档完整度 100%

---

## 📅 详细任务分解

### 任务4.1：模块管理API开发（2天）

#### 4.1.1 数据库设计（0.5天）
**时间**: 3月6日上午

**数据表设计**:

```sql
-- 模块表
CREATE TABLE modules (
  id VARCHAR(36) PRIMARY KEY,
  name VARCHAR(100) NOT NULL COMMENT '模块名称',
  code VARCHAR(50) NOT NULL UNIQUE COMMENT '模块代码',
  description TEXT COMMENT '模块描述',
  icon VARCHAR(100) COMMENT '模块图标',
  version VARCHAR(20) COMMENT '模块版本',
  status ENUM('enabled', 'disabled') DEFAULT 'enabled' COMMENT '状态',
  is_system BOOLEAN DEFAULT FALSE COMMENT '是否系统模块',
  sort_order INT DEFAULT 0 COMMENT '排序',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_code (code),
  INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模块表';

-- 模块配置表
CREATE TABLE module_configs (
  id VARCHAR(36) PRIMARY KEY,
  module_id VARCHAR(36) NOT NULL COMMENT '模块ID',
  config_key VARCHAR(100) NOT NULL COMMENT '配置键',
  config_value TEXT COMMENT '配置值',
  config_type ENUM('string', 'number', 'boolean', 'json') DEFAULT 'string',
  description VARCHAR(255) COMMENT '配置说明',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (module_id) REFERENCES modules(id) ON DELETE CASCADE,
  UNIQUE KEY uk_module_key (module_id, config_key),
  INDEX idx_module_id (module_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模块配置表';
```

**交付物**:
- `backend/database-migrations/create-modules-tables.sql`

#### 4.1.2 实体类开发（0.5天）
**时间**: 3月6日下午

**实体类**:
- `backend/src/entities/Module.ts`
- `backend/src/entities/ModuleConfig.ts`

**功能**:
- 定义Module实体（id, name, code, description, icon, version, status, isSystem, sortOrder）
- 定义ModuleConfig实体（id, moduleId, configKey, configValue, configType, description）
- 实现getValue/setValue方法处理不同类型的配置值

#### 4.1.3 Service层开发（0.5天）
**时间**: 3月7日上午

**Service类**:
- `backend/src/services/ModuleService.ts`

**方法**:
- `getModules(query)` - 获取模块列表
- `getModuleById(id)` - 获取模块详情
- `createModule(data)` - 创建模块
- `updateModule(id, data)` - 更新模块
- `deleteModule(id)` - 删除模块
- `enableModule(id)` - 启用模块
- `disableModule(id)` - 禁用模块
- `getModuleConfig(moduleId)` - 获取模块配置
- `updateModuleConfig(moduleId, configs)` - 更新模块配置

#### 4.1.4 Controller和路由开发（0.5天）
**时间**: 3月7日下午

**Controller**:
- `backend/src/controllers/admin/ModuleController.ts`

**路由**:
- `backend/src/routes/admin/modules.ts`

**API端点**:
```typescript
GET    /api/v1/admin/modules              // 获取模块列表
GET    /api/v1/admin/modules/:id          // 获取模块详情
POST   /api/v1/admin/modules              // 创建模块
PUT    /api/v1/admin/modules/:id          // 更新模块
DELETE /api/v1/admin/modules/:id          // 删除模块
POST   /api/v1/admin/modules/:id/enable   // 启用模块
POST   /api/v1/admin/modules/:id/disable  // 禁用模块
GET    /api/v1/admin/modules/:id/config   // 获取模块配置
PUT    /api/v1/admin/modules/:id/config   // 更新模块配置
```

---

### 任务4.2：系统设置API开发（2天）

#### 4.2.1 数据库设计（0.5天）
**时间**: 3月8日上午

**数据表设计**:

```sql
-- 系统设置表（已存在system_config，需要扩展）
-- 添加新的配置项

-- 管理员用户表
CREATE TABLE admin_users (
  id VARCHAR(36) PRIMARY KEY,
  username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  password VARCHAR(255) NOT NULL COMMENT '密码（加密）',
  email VARCHAR(100) COMMENT '邮箱',
  phone VARCHAR(20) COMMENT '手机号',
  real_name VARCHAR(50) COMMENT '真实姓名',
  avatar VARCHAR(255) COMMENT '头像',
  role ENUM('super_admin', 'admin', 'operator') DEFAULT 'operator' COMMENT '角色',
  status ENUM('active', 'inactive', 'locked') DEFAULT 'active' COMMENT '状态',
  last_login_at TIMESTAMP NULL COMMENT '最后登录时间',
  last_login_ip VARCHAR(50) COMMENT '最后登录IP',
  login_count INT DEFAULT 0 COMMENT '登录次数',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_username (username),
  INDEX idx_status (status),
  INDEX idx_role (role)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='管理员用户表';

-- 管理员操作日志表
CREATE TABLE admin_operation_logs (
  id VARCHAR(36) PRIMARY KEY,
  admin_id VARCHAR(36) NOT NULL COMMENT '管理员ID',
  admin_name VARCHAR(50) COMMENT '管理员名称',
  operation VARCHAR(100) NOT NULL COMMENT '操作类型',
  module VARCHAR(50) COMMENT '操作模块',
  description TEXT COMMENT '操作描述',
  request_method VARCHAR(10) COMMENT '请求方法',
  request_url VARCHAR(255) COMMENT '请求URL',
  request_params TEXT COMMENT '请求参数',
  response_status INT COMMENT '响应状态码',
  ip_address VARCHAR(50) COMMENT 'IP地址',
  user_agent TEXT COMMENT 'User Agent',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_admin_id (admin_id),
  INDEX idx_operation (operation),
  INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='管理员操作日志表';
```

**交付物**:
- `backend/database-migrations/create-admin-users-table.sql`
- `backend/database-migrations/create-admin-operation-logs-table.sql`

#### 4.2.2 实体类开发（0.5天）
**时间**: 3月8日下午

**实体类**:
- `backend/src/entities/AdminUser.ts`
- `backend/src/entities/AdminOperationLog.ts`

#### 4.2.3 Service层开发（0.5天）
**时间**: 3月9日上午

**Service类**:
- `backend/src/services/AdminUserService.ts`
- `backend/src/services/SystemSettingsService.ts`

**AdminUserService方法**:
- `getAdminUsers(query)` - 获取管理员列表
- `getAdminUserById(id)` - 获取管理员详情
- `createAdminUser(data)` - 创建管理员
- `updateAdminUser(id, data)` - 更新管理员
- `deleteAdminUser(id)` - 删除管理员
- `changePassword(id, oldPassword, newPassword)` - 修改密码
- `resetPassword(id, newPassword)` - 重置密码
- `lockUser(id)` - 锁定用户
- `unlockUser(id)` - 解锁用户

**SystemSettingsService方法**:
- `getSettings(category)` - 获取系统设置
- `updateSettings(category, settings)` - 更新系统设置
- `getOperationLogs(query)` - 获取操作日志
- `logOperation(data)` - 记录操作日志

#### 4.2.4 Controller和路由开发（0.5天）
**时间**: 3月9日下午

**Controller**:
- `backend/src/controllers/admin/AdminUserController.ts`
- `backend/src/controllers/admin/SystemSettingsController.ts`

**路由**:
- `backend/src/routes/admin/admin-users.ts`
- `backend/src/routes/admin/system-settings.ts`

**API端点**:
```typescript
// 管理员用户管理
GET    /api/v1/admin/admin-users                    // 获取管理员列表
GET    /api/v1/admin/admin-users/:id                // 获取管理员详情
POST   /api/v1/admin/admin-users                    // 创建管理员
PUT    /api/v1/admin/admin-users/:id                // 更新管理员
DELETE /api/v1/admin/admin-users/:id                // 删除管理员
PUT    /api/v1/admin/admin-users/:id/password       // 修改密码
POST   /api/v1/admin/admin-users/:id/reset-password // 重置密码
POST   /api/v1/admin/admin-users/:id/lock           // 锁定用户
POST   /api/v1/admin/admin-users/:id/unlock         // 解锁用户

// 系统设置
GET    /api/v1/admin/system-settings                // 获取系统设置
PUT    /api/v1/admin/system-settings                // 更新系统设置
GET    /api/v1/admin/system-settings/logs           // 获取操作日志
```

---

### 任务4.3：接口管理API开发（1天）

#### 4.3.1 数据库设计（0.25天）
**时间**: 3月10日上午

**数据表设计**:

```sql
-- API配置表
CREATE TABLE api_configs (
  id VARCHAR(36) PRIMARY KEY,
  name VARCHAR(100) NOT NULL COMMENT 'API名称',
  code VARCHAR(50) NOT NULL UNIQUE COMMENT 'API代码',
  description TEXT COMMENT 'API描述',
  api_key VARCHAR(100) NOT NULL UNIQUE COMMENT 'API密钥',
  api_secret VARCHAR(255) NOT NULL COMMENT 'API密钥（加密）',
  status ENUM('active', 'inactive') DEFAULT 'active' COMMENT '状态',
  rate_limit INT DEFAULT 1000 COMMENT '速率限制（次/小时）',
  allowed_ips TEXT COMMENT '允许的IP（JSON数组）',
  expires_at TIMESTAMP NULL COMMENT '过期时间',
  last_used_at TIMESTAMP NULL COMMENT '最后使用时间',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_api_key (api_key),
  INDEX idx_status (status),
  INDEX idx_code (code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='API配置表';

-- API调用日志表
CREATE TABLE api_call_logs (
  id VARCHAR(36) PRIMARY KEY,
  api_config_id VARCHAR(36) COMMENT 'API配置ID',
  api_key VARCHAR(100) COMMENT 'API密钥',
  endpoint VARCHAR(255) NOT NULL COMMENT '调用端点',
  method VARCHAR(10) NOT NULL COMMENT '请求方法',
  request_params TEXT COMMENT '请求参数',
  response_status INT COMMENT '响应状态码',
  response_time INT COMMENT '响应时间（ms）',
  ip_address VARCHAR(50) COMMENT 'IP地址',
  user_agent TEXT COMMENT 'User Agent',
  error_message TEXT COMMENT '错误信息',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_api_config_id (api_config_id),
  INDEX idx_api_key (api_key),
  INDEX idx_created_at (created_at),
  INDEX idx_endpoint (endpoint)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='API调用日志表';
```

**交付物**:
- `backend/database-migrations/create-api-configs-tables.sql`

#### 4.3.2 实体类和Service开发（0.25天）
**时间**: 3月10日上午

**实体类**:
- `backend/src/entities/ApiConfig.ts`
- `backend/src/entities/ApiCallLog.ts`

**Service类**:
- `backend/src/services/ApiConfigService.ts`

**方法**:
- `getApiConfigs(query)` - 获取API配置列表
- `getApiConfigById(id)` - 获取API配置详情
- `createApiConfig(data)` - 创建API配置
- `updateApiConfig(id, data)` - 更新API配置
- `deleteApiConfig(id)` - 删除API配置
- `regenerateKey(id)` - 重新生成密钥
- `getApiCallLogs(query)` - 获取API调用日志
- `logApiCall(data)` - 记录API调用

#### 4.3.3 Controller和路由开发（0.25天）
**时间**: 3月10日下午

**Controller**:
- `backend/src/controllers/admin/ApiConfigController.ts`

**路由**:
- `backend/src/routes/admin/api-configs.ts`

**API端点**:
```typescript
GET    /api/v1/admin/api-configs                    // 获取API配置列表
GET    /api/v1/admin/api-configs/:id                // 获取API配置详情
POST   /api/v1/admin/api-configs                    // 创建API配置
PUT    /api/v1/admin/api-configs/:id                // 更新API配置
DELETE /api/v1/admin/api-configs/:id                // 删除API配置
POST   /api/v1/admin/api-configs/:id/regenerate-key // 重新生成密钥
GET    /api/v1/admin/api-configs/:id/logs           // 获取API调用日志
GET    /api/v1/admin/api-configs/logs               // 获取所有API调用日志
```

#### 4.3.4 API中间件开发（0.25天）
**时间**: 3月10日下午

**中间件**:
- `backend/src/middleware/apiKeyAuth.ts` - API密钥认证
- `backend/src/middleware/apiRateLimit.ts` - API速率限制

---

### 任务4.4：前后端对接（1天）

#### 4.4.1 模块管理页面对接（0.25天）
**时间**: 3月11日上午

**页面**:
- `admin/src/views/modules/List.vue`
- `admin/src/views/modules/Config.vue`

**功能**:
- 对接模块列表API
- 对接模块启用/禁用API
- 对接模块配置API
- 实现模块创建/编辑对话框
- 实现模块配置对话框

#### 4.4.2 系统设置页面对接（0.25天）
**时间**: 3月11日上午

**页面**:
- `admin/src/views/settings/Basic.vue`（已完成部分）
- `admin/src/views/settings/AdminUsers.vue`

**功能**:
- 对接系统设置API
- 对接管理员用户管理API
- 实现管理员创建/编辑对话框
- 实现密码修改功能
- 实现操作日志查看

#### 4.4.3 接口管理页面对接（0.25天）
**时间**: 3月11日下午

**页面**:
- `admin/src/views/api/List.vue`

**功能**:
- 对接API配置列表API
- 对接API密钥生成API
- 实现API配置创建/编辑对话框
- 实现API调用日志查看
- 实现API密钥复制功能

#### 4.4.4 API方法封装（0.25天）
**时间**: 3月11日下午

**文件**:
- `admin/src/api/admin.ts`（扩展）

**新增方法**:
```typescript
// 模块管理
export const getModules = (params) => request.get('/modules', { params })
export const getModuleById = (id) => request.get(`/modules/${id}`)
export const createModule = (data) => request.post('/modules', data)
export const updateModule = (id, data) => request.put(`/modules/${id}`, data)
export const deleteModule = (id) => request.delete(`/modules/${id}`)
export const enableModule = (id) => request.post(`/modules/${id}/enable`)
export const disableModule = (id) => request.post(`/modules/${id}/disable`)
export const getModuleConfig = (id) => request.get(`/modules/${id}/config`)
export const updateModuleConfig = (id, data) => request.put(`/modules/${id}/config`, data)

// 管理员用户
export const getAdminUsers = (params) => request.get('/admin-users', { params })
export const getAdminUserById = (id) => request.get(`/admin-users/${id}`)
export const createAdminUser = (data) => request.post('/admin-users', data)
export const updateAdminUser = (id, data) => request.put(`/admin-users/${id}`, data)
export const deleteAdminUser = (id) => request.delete(`/admin-users/${id}`)
export const changeAdminPassword = (id, data) => request.put(`/admin-users/${id}/password`, data)
export const resetAdminPassword = (id, data) => request.post(`/admin-users/${id}/reset-password`, data)

// 系统设置
export const getSystemSettings = () => request.get('/system-settings')
export const updateSystemSettings = (data) => request.put('/system-settings', data)
export const getOperationLogs = (params) => request.get('/system-settings/logs', { params })

// API配置
export const getApiConfigs = (params) => request.get('/api-configs', { params })
export const getApiConfigById = (id) => request.get(`/api-configs/${id}`)
export const createApiConfig = (data) => request.post('/api-configs', data)
export const updateApiConfig = (id, data) => request.put(`/api-configs/${id}`, data)
export const deleteApiConfig = (id) => request.delete(`/api-configs/${id}`)
export const regenerateApiKey = (id) => request.post(`/api-configs/${id}/regenerate-key`)
export const getApiCallLogs = (params) => request.get('/api-configs/logs', { params })
```

---

### 任务4.5：测试和文档（1天）

#### 4.5.1 单元测试（0.25天）
**时间**: 3月12日上午

**测试文件**:
- `backend/test-modules-api.js`
- `backend/test-admin-users-api.js`
- `backend/test-api-configs-api.js`

**测试内容**:
- 所有API端点的CRUD操作
- 权限验证
- 数据验证
- 错误处理

#### 4.5.2 集成测试（0.25天）
**时间**: 3月12日上午

**测试场景**:
- 创建模块 → 配置模块 → 启用模块 → 禁用模块
- 创建管理员 → 修改密码 → 锁定用户 → 解锁用户
- 创建API配置 → 调用API → 查看日志 → 重新生成密钥

#### 4.5.3 前端功能测试（0.25天）
**时间**: 3月12日下午

**测试内容**:
- 所有页面的UI交互
- 表单验证
- 数据加载和刷新
- 错误提示
- 成功提示

#### 4.5.4 文档编写（0.25天）
**时间**: 3月12日下午

**文档**:
- API文档（Swagger/OpenAPI格式）
- 数据库设计文档
- 用户操作手册
- 第四阶段完成总结

---

## 📊 进度跟踪

### 每日进度表

| 日期 | 任务 | 预计工时 | 实际工时 | 完成度 | 备注 |
|------|------|---------|---------|--------|------|
| 3月6日 | 模块管理数据库+实体 | 1天 | - | 0% | - |
| 3月7日 | 模块管理Service+API | 1天 | - | 0% | - |
| 3月8日 | 系统设置数据库+实体 | 1天 | - | 0% | - |
| 3月9日 | 系统设置Service+API | 1天 | - | 0% | - |
| 3月10日 | 接口管理完整开发 | 1天 | - | 0% | - |
| 3月11日 | 前后端对接 | 1天 | - | 0% | - |
| 3月12日 | 测试和文档 | 1天 | - | 0% | - |

### 里程碑

- [ ] 里程碑1：模块管理API完成（3月7日）
- [ ] 里程碑2：系统设置API完成（3月9日）
- [ ] 里程碑3：接口管理API完成（3月10日）
- [ ] 里程碑4：前后端对接完成（3月11日）
- [ ] 里程碑5：测试通过（3月12日）
- [ ] 里程碑6：第四阶段完成（3月12日）

---

## 🎯 验收标准

### 功能验收
- [ ] 所有API端点正常工作
- [ ] 所有前端页面正常显示
- [ ] 所有功能正常交互
- [ ] 数据正确保存和读取
- [ ] 权限控制正确

### 性能验收
- [ ] API响应时间 < 500ms
- [ ] 列表查询 < 300ms
- [ ] 详情查询 < 200ms
- [ ] 页面加载 < 2s

### 质量验收
- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试通过率 100%
- [ ] 代码审查通过
- [ ] 文档完整

---

## ⚠️ 风险和应对

### 风险1：数据库设计复杂
**影响**: 可能延期0.5天  
**概率**: 中  
**应对**: 提前review设计,参考现有表结构

### 风险2：API开发工作量大
**影响**: 可能延期1天  
**概率**: 中  
**应对**: 复用现有代码模式,提高开发效率

### 风险3：前后端对接问题
**影响**: 可能延期0.5天  
**概率**: 低  
**应对**: 及时沟通,提前定义接口规范

---

## 📝 交付清单

### 后端交付物
- [ ] 数据库迁移脚本（3个）
- [ ] 实体类（5个）
- [ ] Service类（4个）
- [ ] Controller类（4个）
- [ ] 路由文件（4个）
- [ ] 中间件（2个）
- [ ] 测试脚本（3个）

### 前端交付物
- [ ] API方法封装（30+个方法）
- [ ] 页面对接（3个页面）
- [ ] 对话框组件（5+个）

### 文档交付物
- [ ] API文档
- [ ] 数据库设计文档
- [ ] 用户操作手册
- [ ] 测试报告
- [ ] 第四阶段完成总结

---

## 🚀 开始实施

**准备工作**:
1. ✅ 确认开发环境正常
2. ✅ 确认数据库连接正常
3. ✅ 确认前后端服务运行正常
4. ⏳ Review现有代码结构
5. ⏳ 准备开发工具和资源

**立即开始**:
- 任务4.1.1：模块管理数据库设计
- 预计完成时间：3月6日上午

---

**实施计划制定完成！准备开始第四阶段开发！**
